protocol_version: 3

task_id: TASK-0020
task_revision: 1
state: APPROVED

origin:
  type: accepted_follow_up
  source: "agent-skills/TASK-0031 review@a9fc6c47b7c878d525d9d3ebfde54af03547d887"

architect_binding:
  target_repository: phatnguyen03022001/architect-profile

target:
  repository: phatnguyen03022001/architect-profile
  branch:
    name: dev
    role: integration

execution_base:
  mode: handoff_snapshot
  capture: after_final_planning_commit
  require_exact_match: true

skill_library:
  repository: phatnguyen03022001/agent-skills
  revision: "4a063c41f429207577434600a3571dc17822cd78"

execution_skills:
  required:
    - executor
    - verification
    - simplicity
    - github-workflow

authority_sources:
  - source: "architect-profile/main@89dacd43b7a200a7b9cd36d9545f23c930eb4f44"
    role: "currently active ecosystem authority-set"
    precedence: 1
  - source: "agent-skills/main@db03b97b5c42c9c64ad8dce76bc694530e41a206"
    role: "accepted stable support revision to bind"
    precedence: 2
  - source: "agent-skills/TASK-0031/review.yaml@a9fc6c47b7c878d525d9d3ebfde54af03547d887"
    role: "durable acceptance of exact stable promotion"
    precedence: 3

objective: >-
  Produce the smallest architect-profile activation candidate that changes only the agent-skills
  pin in authority.lock from 4a063c41f429207577434600a3571dc17822cd78 to
  db03b97b5c42c9c64ad8dce76bc694530e41a206, while preserving all other support pins and all
  bootstrap, Case Router, execution-surface, role, model, and fail-closed semantics byte-for-byte.

scope:
  required_changes:
    - "Update only .agent/bootstrap/authority.lock.json so agent-skills resolves exactly db03b97b5c42c9c64ad8dce76bc694530e41a206."
    - "Preserve exact agent-standards, agent-documents, and agent-runtime pins."
    - "Freshly prove the locked agent-skills revision exists, equals stable main, contains the canonical Case Router, and reconstructs the expected selective-context bootstrap without chat state."
    - "Run the existing architect-profile bootstrap/validator regression against the candidate lock; do not add semantics or another validation framework."

invariants:
  - "architect-profile/main remains unchanged in this task; this candidate is not active until a later explicit activation task promotes the accepted candidate."
  - "authority-set identity remains architect-profile/main@P; architect-profile does not self-pin."
  - "Case Router remains exactly EXECUTE -> executor."
  - "CHATGPT surfaces remain GPT-5.6 Sol/HIGH and CODEX surfaces remain LUNA/XHIGH."
  - "Agent Runtime remains transport only for CHATGPT_LOCAL."
  - "Topology remains exactly {dev, main}."

forbidden_changes:
  - "Do not modify bootstrap.json, bootstrap code, documentation, capability routing, Case Router, roles, protocol, task model, execution surfaces, model policy, or support repositories."
  - "Do not change any support pin other than agent-skills."
  - "Do not mutate architect-profile/main."
  - "Do not create/delete/rename branches, tags, releases, services, registries, caches, schedulers, or new dependencies."

capability_requirements:
  EXECUTION:
    - repository_content_read
    - repository_content_write
    - native_repository_execution
    - git_history_read
    - git_commit
    - git_push
    - remote_ref_read
  REVIEW:
    - repository_content_read
    - repository_content_write
    - exact_commit_report_resolution
    - remote_ref_read

acceptance_criteria:
  - id: AC-1
    requirement: "Candidate changes only the agent-skills authority.lock revision to db03b97b5c42c9c64ad8dce76bc694530e41a206 and preserves all other lock bytes semantically unchanged."
    evidence_required: "Exact base-to-candidate diff and lock parse."
  - id: AC-2
    requirement: "The new pin resolves immutably, equals agent-skills/main at verification time, and its Case Router remains exactly EXECUTE -> executor."
    evidence_required: "Fresh remote ref plus exact locked-object/router proof."
  - id: AC-3
    requirement: "Existing bootstrap reconstruction and validator/tests remain green with no chat history and no new semantics."
    evidence_required: "One bounded deterministic verification bundle using existing repo-native checks plus no-chat reconstruction."
  - id: AC-4
    requirement: "No support pin other than agent-skills, reusable semantics, branch topology, or architect-profile/main is changed."
    evidence_required: "Exact diff/ref/topology proof and simplicity review."
  - id: AC-5
    requirement: "Exact implementation candidate and compact report child are published on dev with fresh remote proof and final local hygiene."
    evidence_required: "Candidate/report SHAs, remote dev/main/topology, and local closure evidence."

verification:
  executor_checks:
    - id: CHECK-1
      command_or_check: "Fresh-resolve exact task/base, current active architect-profile/main authority-set, accepted agent-skills stable revision/review, repository identity, dev/main/topology, local ownership, and capabilities."
      required: true
    - id: CHECK-2
      command_or_check: "Change only the one agent-skills revision in authority.lock and prove exact candidate diff."
      required: true
    - id: CHECK-3
      command_or_check: "Run one bounded deterministic bundle covering existing bootstrap/validator tests, lock resolution, exact Case Router, no-chat reconstruction, py_compile where applicable, and git diff --check."
      required: true
    - id: CHECK-4
      command_or_check: "Publish candidate then compact report child on dev; fresh-prove main unchanged/topology {dev, main} and final local hygiene."
      required: true

  authoritative_verification:
    required: false
    mechanism: ""
    expected_signal: ""

git_authority:
  create_branch: false
  commit: true
  push: true
  promote_to_main: false

release_authority:
  create_version_tag: false
  mutate_repository_metadata: false
  publish_release: false

blocking_decisions: []
execution_ready: true
