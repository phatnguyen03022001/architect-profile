| ID | Decision for this snapshot | Reversal / next gate |
|---|---|---|
| G1 | Stable active generation is the supplied `main` refs: profile `d471…`, skills `ed8d…`, runtime `c49d…`. Supplied dev refs/candidates are not stable activation and must not be promoted or treated as locks. | Fresh GitHub resolution plus separately authorized candidate capture/promotion; no main/staging/force push/branch creation. |
| G2 | Durable routing remains exactly Codex Luna/medium and ChatGPT Sol/high; durable Architect is ChatGPT. The operator’s appointment of root Codex as governing Architect and Terra/high for delegated Executors is a session-only explicit override, not a profile edit. | Persist any future-reliant material intent as L1 in its narrow canonical owner; otherwise it expires with this session. |
| G3 | The legal next work is narrowly resume `TASK-0032` revision 3 on its stated projection/report-only scope, after normal locked bootstrap → exact Case Router → `EXECUTE → executor`. It may correct the two closure predicates and supplied evidence references, but cannot declare the five-repo debt closed. `TASK-0044` is approved planning only at supplied `7d7…`, has no report, and is outside the active skills lock `ed8…`; do not overlap or execute it. | A freshly resolved, exact target authority and non-overlapping task binding for `TASK-0044`; then its own execution and independent acceptance. |
| G4 | Runtime WIP disposition is closed on dev: `TASK-0022` is accepted, backup evidence is retained, env/venv/operator state preserved, and no confinement source was published. `TASK-0019` remains withdrawn, not completed or re-opened. | Only an explicit newly scoped task plus observed need may revisit confinement; backup is recovery evidence, never authority. |
| G5 | Local dirty/ahead state is unknown divergence: neither push, reset, deletion, nor adoption is legal. Preserve real `.env`, venv, and operator state; never inspect opaque real values. | Fresh remote/branch identity and an authorized, bounded reconciliation/disposition procedure. |
| G6 | Reuse immutable accepted evidence for unchanged historical facts: `TASK-0043` accepted at `251d…`; runtime `TASK-0022` accepted at `6f23…`; supplied dev acceptances are observations. Mutable gates—remote refs, topology, branch policy, write authority, and post-publication equality—require fresh GitHub observation immediately when a consequence depends on them. This packet proves no such refresh occurred. | Refresh remote facts before any canonical mutation/publication; remote drift fails closed. |
| G7 | Load only the provided profile commit, bootstrap/lock, exact router at the lock, current target binding/contract, and capability path; expand only for missing or contradictory evidence. Do not preload broad trees, history, PROGRAM, or raw chat. L0 is private/non-authoritative; operator intent is L1 and must be canonically persisted before future reliance; learned reusable judgment belongs only in calibration. | Explicit provenance/reconstruction need, or missing authority/evidence. |
| G8 | Not proven: independent cold-successor convergence after any narrow correction; a natural new IELTS run under its own exact later authority; final five-repository live governance/debt sweep with no admitted MUST_CLOSE finding; final dev authority-set selection; and any stable-main activation/freeze/handoff. PROGRAM is optional navigation only—no lifecycle, mutable debt status, or source authority. Canonical owner task/review evidence decides closure. | Obtain each named independent acceptance/fresh sweep under its exact authority. |

| Scenario | Decision now | Concrete reversal trigger |
|---|---|---|
| S1: 100 requests/day, no measured bottleneck | Do not add Redis. | Measured performance/capacity evidence or an authoritative current requirement proves the existing design insufficient. |
| S2: async email, durability/retry guarantees unspecified | Do not choose a queue yet; clarify the required delivery semantics. | An authoritative requirement specifies durable asynchronous delivery/retries or equivalent guarantees the simple path cannot satisfy. |
| S3: one service, no platform constraint | Do not add Kubernetes. | A concrete current operational/platform requirement or measured operational insufficiency justifies it. |
| S4: temporary best-effort cache, authority elsewhere | No persistent infrastructure required. Use the least complex reversible cache approach compatible with current constraints. | Cache must survive failures/restarts, become authoritative, or has proven performance/availability requirements requiring persistence. |
| S5: forecast 1M users, low current load | Do not prebuild topology. Retain the forecast as a capacity/observability envelope with triggers. | Measured approaching limits or a concrete near-term authoritative requirement proves the simple topology materially insufficient. |